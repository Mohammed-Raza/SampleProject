enum GroceryType { fruits, veggies, milkProducts, cookies }

enum RequestType { get, put, post, delete }

enum DataErrorStateType {
  noInternet,
  timeoutException,
  serverNotFound,
  somethingWentWrong,
  badRequest,
  unauthorized,
  pageNotFound,
  none
}

enum ThemeType { light, dark, system }

enum DeviceResolutionType { mobile, tab, desktop }
